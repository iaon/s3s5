# Benchmark Results

Committed files in this directory are stable protocol v1 baselines.

Local ad hoc results should be written under `benchmarks/results/local/`; that
directory is ignored by Git.

The JSON schema is emitted by `s3s5-perf` and contains only aggregate metrics.
It does not include credentials, PSKs, target hostnames, session IDs, or full
object keys.

